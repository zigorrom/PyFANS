<Window x:Class="RTS_analysis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RTS_analysis"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Menu Margin="0,0,0.4,0.2">
            <MenuItem Header="File" >
                <MenuItem Command="{Binding OpenFiles, Mode=OneWay}" Header="Open" HorizontalAlignment="Left" Width="139.2"/>
                <Separator HorizontalAlignment="Left" Width="139.2"/>
                <MenuItem Header="Current Folder" ToolTip="{Binding WorkingDirectory}" HorizontalAlignment="Left" Width="139.2"/>
            </MenuItem>
            <MenuItem Command="{Binding StartProcessing, Mode=OneWay}" Header="Process"/>
        </Menu>
        <StatusBar Grid.Row="2"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Filenames}">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
